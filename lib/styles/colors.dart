import 'package:flutter/material.dart';

const primaryColor = Color(0xFF7e257e);
const backgroundColor = Color(0xFF2a2664);
const greenColor = Color(0xFF50bf9d);
const textWhite = Color(0xFFffffff);
const textBlack = Color(0xFF000000);

const orageColor = Color(0xFFEB8C1F);

const appBgColor = RadialGradient(
    colors: [primaryColor, backgroundColor, backgroundColor],
    radius: 4,
    center: Alignment.topLeft);
